import React from "react";
import { View, Text } from "react-native";

function Saved() {
    return(
        <View>
            <Text style={{backgroundColor: 'green'}}>FavouriteScreen</Text>
        </View>
    );
}

export default Saved;

/* 

Extra feature select word that you want to change and click ctrl+d, and it will select same words so you can change evrithing at once!

*/